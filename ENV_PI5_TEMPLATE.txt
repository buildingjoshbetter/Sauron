# API Keys
OPENROUTER_API_KEY=your_openrouter_key_here

# LLM Routing (Optimized for SPEED + QUALITY, using paid tiers)
# Simple queries (greetings, basic Q&A): ~0.8s response
OPENROUTER_FAST_MODEL=openai/gpt-4o-mini
# Medium queries (general questions): ~1.5s response
OPENROUTER_MEDIUM_MODEL=openai/gpt-4o-mini
# Complex queries (memory recall, analysis): ~2-3s response
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# Ultra queries (multi-step reasoning): ~4-6s response
OPENROUTER_ULTRA_MODEL=openai/gpt-4o
# Genius queries (deep research, cross-referencing): ~10-15s response
OPENROUTER_GENIUS_MODEL=openai/o1-preview
OPENAI_API_KEY=your_openai_key_here
TWILIO_ACCOUNT_SID=your_twilio_sid_here
TWILIO_AUTH_TOKEN=your_twilio_token_here
TWILIO_FROM_NUMBER=+1234567890
TWILIO_TO_NUMBER=+1234567890

# Audio settings (USB Mic)
AUDIO_SAMPLE_RATE=16000
AUDIO_CHUNK_SECONDS=5
AUDIO_DEVICE=plughw:1,0  # Update after running 'arecord -l' to find your USB mic

# Vision settings
ENABLE_VISION=true
MOTION_SENSITIVITY=0.08
CAMERA_SNAPSHOT_WIDTH=1920
CAMERA_SNAPSHOT_HEIGHT=1080
ENABLE_VIDEO_ON_MOTION=true
VIDEO_DURATION_SECONDS=10

# SMS behavior
SEND_SMS_ON_QUESTIONS=true
SEND_SMS_ON_MOTION=false
ENABLE_STREAMING_SMS=false

# Device info
DEVICE_NAME=pi5-sauron
DATA_DIR=/home/pi/sauron_data
LOG_LEVEL=INFO

# Location (for time/weather)
LATITUDE=30.2672
LONGITUDE=-97.7431
TIMEZONE=America/Chicago

# LOCAL Whisper on Pi 5 (MUCH FASTER!)
USE_LOCAL_WHISPER=true
WHISPER_MODEL_SIZE=base
# CRITICAL: Enable streaming for instant trigger word detection (<1s ack SMS!)
ENABLE_STREAMING_TRANSCRIPTION=true
NAS_WHISPER_URL=

# NAS settings (not needed for Pi 5)
# MEMORY_DIR=/mnt/nas/sauron
# NAS_ARCHIVE_DIR=/mnt/nas/sauron

